# 🩺 التوثيق الشامل والتفصيلي للباك إند (Dermalyze Backend Documentation)

هذا الملف يحتوي على شرح **مفصل ودقيق لكل جزء، مجلد، ومسار** تم بناؤه وتطويره في الباك إند الخاص بمشروع **Dermalyze**.

---

## 🚀 1. التقنيات الأساسية (Core Tech Stack)

- **بيئة التشغيل (Runtime):** Node.js
- **إطار العمل (Framework):** Express.js (لبناء الـ APIs).
- **قاعدة البيانات (Database):** MySQL (مع استخدام Sequelize كـ ORM لإدارة الجداول والعلاقات وتحديثها).
- **الاتصال اللحظي (Real-time):** Socket.io (للمحادثات الفورية بين الطبيب والمريض والإشعارات الحية).
- **الذكاء الاصطناعي (AI & Computer Vision):** 
  - تحليل صور الأمراض الجلدية عبر Hugging Face API.
  - سكريبتات Python (`scripts/inference.py`) لتحليل تطور المرض ومقارنة الصور (Severity Metrics).
  - مكتبات Python (`face_recognition` و `pytesseract`) لمطابقة وجه الطبيب مع صورة البطاقة وقراءة النصوص للتأكد من هويته المهنية.
- **تخزين الملفات والصور:** Cloudinary (لرفع الصور بشكل آمن واسترجاعها).
- **تكامل خارجي (Third-Party Integrations):** Google Sheets API (لقاعدة بيانات الأدوية)، Resend API (لإرسال إيميلات الـ OTP).

---

## 📂 2. هيكلة المشروع (Project Structure) والتفاصيل

المشروع مقسم بعناية بنمط **MVC (Model-View-Controller)** ليكون قابلاً للتوسع (Scalable):

### 🗄️ المجلد: `models/` (قواعد البيانات والجداول)
كل ملف هنا يمثل جدولاً في قاعدة البيانات مع علاقاته (Associations) مع الجداول الأخرى:
1. **`User.js`**: يدير حسابات الأطباء والمرضى، كلمات المرور المشفرة، رموز الـ 2FA والـ OTP.
2. **`Patient.js`**: يربط المريض بطبيبه، ويحتفظ بنسبة التعافي وموعد الزيارة القادمة.
3. **`Appointment.js`**: حجز المواعيد بين الطبيب والمريض وحالتها.
4. **`Medication.js`**: الروشتة والأدوية التي يصفها الطبيب.
5. **`Disease.js` & `ClinicalDisease.js`**: قاعدة بيانات الأمراض الجلدية المتاحة بالنظام.
6. **`DiseaseReport.js`**: يوثق تقارير الحالة، الأعراض، وعلامات التحسن التي يكتبها الطبيب.
7. **`Analysis.js`**: نتائج الذكاء الاصطناعي والصور المرفوعة للتحليل.
8. **`Message.js` & `Conversation.js`**: جداول تخزين المحادثات الفورية.
9. **`Notification.js`**: تسجيل الإشعارات (مواعيد، نتائج، تنبيهات).
10. **`Smart*.js` Models**: (الـ Smart History) وهي 7 جداول مترابطة تقوم بتسجيل نسب التحسن الفعلية لكل علاج مع كل مرض، ليقوم النظام باقتراح أفضل دواء للحالات المشابهة.

### 🧠 المجلد: `controllers/` (المنطق البرمجي - Business Logic)
يحتوي على الأكواد التي تستقبل الطلب وتتعامل مع قواعد البيانات وترجع الرد (Response):
1. **`authController.js`**: التسجيل، تسجيل الدخول، التحقق من الهوية بالذكاء الاصطناعي (KYC)، المصادقة الثنائية (2FA)، وتغيير الباسورد.
2. **`doctorController.js`**: لوحة تحكم الطبيب (جلب مرضاه، حساب العمر ديناميكياً، الإحصائيات، إضافة تقييمات، وجدولة المواعيد).
3. **`patientController.js`**: جلب بيانات المريض السريرية وتحديث نسبة تعافيه (`recoveryProgress`).
4. **`medicationController.js`**: ربط روشتة العلاج بالمريض وتحديثها.
5. **`aiController.js`**: إرسال صور الأمراض الجلدية لـ Hugging Face أو للسكريبت المحلي Python لمقارنتها واستخراج نسبة التحسن.
6. **`chatController.js`**: جلب أرشيف المحادثات السابقة بين طرفين.
7. **`adminController.js`**: الموافقة أو الرفض على طلبات انضمام الأطباء.

### 🛣️ المجلد: `routes/` (المسارات والروابط)
يقوم بتوجيه الروابط (Endpoints) إلى الـ Controllers وتمريرها على الـ Middlewares للحماية:
- `authRoutes.js`, `doctorRoutes.js`, `patientRoutes.js`, `medicationRoutes.js`, `smartHistory.js` وغيرها.

### 🛡️ المجلد: `middlewares/` (الحماية والفلاتر)
يحتوي على فلاتر تعمل قبل تنفيذ أي أمر:
1. **`authMiddleware.js`**: للتأكد من صحة التوكن (JWT).
2. **`roleMiddleware.js`**: للتأكد من صلاحيات المستخدم (مثال: المسار هذا للطبيب فقط).
3. **`verificationMiddleware.js`**: للتأكد أن الطبيب تم قبوله من الإدارة.
4. **`validationMiddleware.js`**: للتحقق من المدخلات (مثلاً: الإيميل صحيح، الباسورد قوي).
5. **`cacheMiddleware.js`**: ذاكرة مؤقتة (Caching) لتسريع استرجاع البيانات مثل الأدوية.
6. **`uploadMiddleware.js`**: معالجة رفع الصور.

### ⚙️ المجلد: `services/` (الخدمات الخارجية والمساعدة)
1. **`socketHandler.js`**: إدارة أحداث الـ WebSockets للمحادثات الحية وإرسال الإشعارات الفورية.
2. **`pythonRunner.js`**: جسر اتصال قوي لتشغيل سكريبتات الـ Python الخاصة بمطابقة الوجوه والمقارنة الطبية.
3. **`emailService.js`**: إرسال الإيميلات الفورية (OTP والقبول/الرفض) باستخدام Resend.

### 🧰 المجلد: `utils/` (الأدوات المساعدة)
- أدوات صغيرة تُستخدم بكثرة مثل إنشاء الإشعارات (`notificationUtils.js`)، تنظيف النصوص، وإنشاء ملفات Excel.

### 📜 المجلد: `scripts/` (نصوص التشغيل والتأسيس)
1. **`seedDiseases.js` & `seedResources.js`**: إدخال البيانات الأساسية (الأمراض والأدوية) في قاعدة البيانات عند أول تشغيل للمشروع.
2. **`inference.py` & `face_match.py`**: سكريبتات البايثون المخصصة للـ Machine Learning.

---

## 🔗 3. كيف يتدفق العمل (Data Flow & Architecture)؟

لو أخذنا مثال: **الطبيب يقوم بجدولة موعد لمريض**
1. **الفرونت إند (Flutter):** يرسل طلب `POST /doctor/patients/:id/appointments` ومع التوكن.
2. **الـ Route (`doctorRoutes.js`):** يستقبل الطلب، يمرره لـ `authMiddleware` (للتأكد من تسجيل الدخول)، ثم `roleMiddleware` (للتأكد أنه طبيب)، ثم `validationMiddleware` (للتأكد من صحة التاريخ والوقت).
3. **الـ Controller (`doctorController.js`):** 
   - يقوم بحفظ الموعد في قاعدة البيانات.
   - يحدّث حالة المريض ليصبح الموعد القادم هو الموعد الجديد.
   - يستدعي خدمة الـ Notifications لحفظ إشعار في الـ Database.
   - يستدعي خدمة `Socket.io` ليرسل إشعار لحظي لهاتف المريض (إذا كان فاتح التطبيق) بأنه "تم حجز موعد جديد لك!".
4. **الرد (Response):** يرجع JSON للفرونت إند بحالة النجاح واسم وتفاصيل الموعد.

---

## 🔒 4. نظام الحماية والأمان المتقدم (Security & Hardening)

- **الحذف التسلسلي (Cascading Deletions):** إذا قرر المريض حذف حسابه، قاعدة البيانات تمحو تلقائياً وبشكل تسلسلي كل محادثاته، صوره، ومواعيده لضمان الخصوصية.
- **التشفير (Encryption):** الباسوردات لا تُحفظ كنصوص، بل تُشفر بصيغة `bcrypt` المعقدة.
- **الـ OTP والـ 2FA:** أمان مضاعف لمنع سرقة الحسابات، برمجناه بحيث الـ OTP ينتهي صلاحيته بعد 10 دقائق بالضبط.
- **تأمين الشبكة:** استخدام مكتبة `helmet` لإخفاء معلومات السيرفر ومنع ثغرات الـ XSS (Cross-Site Scripting).

---

## 🌟 5. المزايا الفريدة التي تم برمجتها بالباك إند (Unique Selling Points)

1. **KYC Automation (التحقق التلقائي للأطباء):** الباك إند مبرمج بأنه بمجرد رفع الطبيب لصورته وصورة رخصته، السيرفر يقرأ النص من الرخصة (OCR) ويقارن وجه الشخص برخصته، مما يمنع انتحال صفة الأطباء.
2. **Smart History (الترشيح الذكي للعلاج):** نظام إحصائي متكامل يبحث في تاريخ العيادة ويوصي الطبيب بالدواء صاحب أعلى نسبة نجاح لحالة المريض الحالية.
3. **Dynamic Object Parsing:** بعض البيانات مثل (العمر - Age) لا تُحفظ في الداتا بيز (لأنها تتغير كل سنة)، بل تُحسب ديناميكياً كل ثانية في الباك إند وتُرسل للفرونت إند.
4. **Morphological Image Comparison:** السيرفر لا يعتمد فقط على الـ APIs الخارجية في الذكاء الاصطناعي، بل لديه محرك Python داخلي يقارن مساحة الالتهاب في صورتين للمريض ليعطي الطبيب نسبة دقيقة للتحسن (مثلاً: الالتهاب قل بنسبة 15.4%).
